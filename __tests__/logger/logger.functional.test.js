import logger from '../../core/logger/logger';
describe('logger functional', () => {
  it('should log and retrieve logs', () => {
    // Ajoutez vos assertions selon le comportement attendu
    expect(logger.log).toBeDefined();
    // Ajoutez d'autres tests d'intégration
  });
});