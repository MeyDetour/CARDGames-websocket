import {ConformiteManager} from '../../core/services/conformiteManager';
describe('conformiteManager', () => {
  it('should check conformity', () => {
    // Ajoutez vos assertions ici
    expect(ConformiteManager.verifyRoomData).toBeDefined();
  });
});