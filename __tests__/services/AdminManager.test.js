import {AdminManager} from '../../core/services/AdminManager';
describe('AdminManager', () => {
  it('should manage admins', () => {
    // Ajoutez vos assertions ici
    expect(AdminManager.createAdmin).toBeDefined();
  });
});